--index-url https://download.pytorch.org/whl/cpu
--extra-index-url https://pypi.org/simple

# CPU torch wheels
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# whisperx pinned to a known-good commit (HEAD at 2026-05-09).
# Bumping this requires re-validating the diarize/transcribe API
# (use_auth_token vs token kwarg has changed before).
whisperx @ git+https://github.com/m-bain/whisperx.git@1c4b23e13d79280a076e98644ac1e38321d253a9

transnetv2-pytorch==1.0.5
opencv-python-headless==4.13.0.92
pillow==12.2.0
librosa==0.11.0
transformers==4.57.6
sentence-transformers==5.4.1
timm==1.0.27
colorlog==6.10.1
scikit-learn==1.8.0
ffmpeg-python==0.2.0
requests==2.33.1
python-dotenv==1.2.2
PyYAML==6.0.3
tmdbv3api==1.9.0
google-generativeai==0.8.6
pika==1.4.0
Flask==3.1.3

# chromadb 1.x is compatible with pydantic 2.9..2.13.
# Bumping this requires also widening the pydantic cap in the api/dev
# requirements; mismatched pins between worker (no cap) and api (<2.10)
# previously let dependency drift go unnoticed.
chromadb==1.5.9
